# Frontend Environment Variables Template
# Copy this file to .env.local and update with your actual values

# The frontend takes no build-time environment variables.
# Set the backend WebSocket URL in the app's Configuration panel at runtime,
# e.g. ws://127.0.0.1:8080 for local development.
